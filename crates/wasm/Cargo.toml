[package]
name = "vectordb-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WebAssembly bindings for vectordb-core (in-memory)"

[dependencies]
vectordb-core = { path = "../core" }
wasm-bindgen = { workspace = true }
serde_json = { workspace = true }

[lib]
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true
